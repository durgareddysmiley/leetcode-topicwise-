📌 Most Common Imports You Forget (IMPORTANT)
1️⃣ Counter 
from collections import Counter 

2️⃣ defaultdict
from collections import defaultdict

3️⃣ deque
from collections import deque

4️⃣ heapq
import heapq

5️⃣ math functions
import math 

## Math functions 
🔢 Most Important Math Functions (YOU SHOULD KNOW)
1️⃣ math.sqrt(x) → Square root
import math
print(math.sqrt(16))   # 4.0

2️⃣ math.floor(x) → Round DOWN
import math
print(math.floor(4.9))   # 4

3️⃣ math.ceil(x) → Round UP
import math
print(math.ceil(4.1))    # 5

4️⃣ math.gcd(a, b) → Greatest Common Divisor
import math
print(math.gcd(12, 18))  # 6

5️⃣ math.pow(a, b) → Power
import math
print(math.pow(2, 3))    # 8.0

6️⃣ math.factorial(n) → n!
import math
print(math.factorial(5))  # 120

❗ Important Difference (Very Useful)
❌ Without math module
x = 5 / 2
print(x)   # 2.5

✅ With math functions
import math
print(math.floor(5/2))  # 2
print(math.ceil(5/2))   # 3

##
SOMETIMES APPEND NOT WORKS TRY EXTEND
